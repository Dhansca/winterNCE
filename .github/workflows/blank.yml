name: firstworkflow
on: workflow_dispatch
jobs:
  first-job:
#    runs-on: ubuntu-latest
    runs-on: self-hosted
    steps:
      - name: print greeting
        run: echo "hello"
      - name: Createfile
        run: touch first.txt
        
  
